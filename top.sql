/* TOP

TOP delimita o número de itens que aparecem em uma busca SELECT.
O SELECT busca todos os itens do critério passado (pode ser milhares), com o TOP especificamos o total q queremos.
*/

EX:

SELECT TOP 100 *
FROM TABELA QUE SE QUER TRABALHAR

SELECT TOP 10 *
FROM production.product